"Java script".length 

// це (length) визначає скільки елементів в одному рядку (букв, пропусків)

let myNamE = "Poly";

let numbeR = 10; 

// використовуємо let тільки тоді, коли знаємо що будемо перевикористовувати
// одинакових імен не може бути. кожне повинне бути унікальним
// не можна писати console.log(number), її значення ДО let number = 10.
// якщо треба написати перед name ще одне слово, name пишемо з великої букви. приклад: myName
// і так далі. просто так не пишемо. потрібно писати з let. приклад вище

console.log(1);
console.log(numbeR);
console.log("Hey");

// так можна писати, коли я хочу побачити щось в консолі 

 const number = 15;
console.log(number);

// так потрібно робити, коли я хочу щоб далі по коду число змінилось, з 10 на 15 наприклад

// ПРИМІТИВНІ 

// number
const age = 25;
console.log(age);

// string
const userName = "Chelsy";
console.log(userName);

// booLean

const isOnline = true;
console.log(isOnline);

// undefined

console.log(undefined);

// null

console.log(null);

// noteandnumber

console.log(NaN);


// спочатку краще створювати через const. якщо далі потрібно буде змінити age, повертаємося і пишемо let
//  маленька практика

const myName = "nastya";
console.log(myName);

const numbere = 6;
console.log(numbere);

const isActive = false;
console.log(isActive);

// стрінг, числове значення, - true. інше - false (число 0, "", пустий рядок також).


